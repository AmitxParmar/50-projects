const buttons = document.querySelectorAll('.ripple')
buttons.forEach(button => {
    button.addEventListener('click', (e) => {
        const x = e.pageX
        const y = e.pageY

        const buttonTop = <e className="target offsetTop">const buttonLeft</e>
    })
})